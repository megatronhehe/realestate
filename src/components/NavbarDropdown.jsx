import React from "react";

const NavbarDropdown = () => {
	return (
		<section className="fixed top-0 left-0 w-full h-full bg-black">
			NavbarDropdown
		</section>
	);
};

export default NavbarDropdown;
